class UserInfo {
    constructor({usersName, usersInfo}) {
        this._userName = usersName;
        this._userInfo = usersInfo;
    }

    getUserInfo() {
        return {}
    }

    setUserInfo() {
        
    }
}