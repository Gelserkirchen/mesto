class Popup {
    constructor(popupSelector) {
        this._popup = document.querySelector(popupSelector);
    }

    open() { // открыть попап
        
    }

    close() { // закрыть попап

    }

    _handleEscCLose() { // логика закрытия попапа через Esc

    }

    setEventListeners() { // добавляет слушатель клика иконке закрытия попапа

    }
}